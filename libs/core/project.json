{
  "name": "@glyphmark/core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/core/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project @glyphmark/core --web",
  "targets": {
    "test:visual": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config libs/core/vitest.config.ts libs/core/test/visual/visual.test.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "test:update-goldens": {
      "executor": "nx:run-commands",
      "options": {
        "command": "UPDATE_SNAPSHOTS=1 vitest run --config libs/core/vitest.config.ts libs/core/test/visual/visual.test.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "test:import-goldens": {
      "executor": "nx:run-commands",
      "options": {
        "command": "IMPORT_GOLDENS=1 vitest run --config libs/core/vitest.config.ts libs/core/test/visual/visual.test.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
